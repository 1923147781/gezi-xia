# 鸽子侠

## 本地开发
```bash
npm install
npm run dev
```

## 环境变量
复制 `.env.local.example` 到 `.env.local` 并填写 Supabase 信息。

## 数据库
在 Supabase SQL Editor 执行 `supabase/schema.sql`。

## 部署
- 推送到 GitHub
- 导入到 Vercel
- 填写 Supabase 环境变量
- 部署
